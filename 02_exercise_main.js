const utils = require("./02_exercise_utils");

function performCalculations() {
  console.log("Adding 5 + 3:", utils.add(5, 3));
  console.log("Subtracting 5 - 3:", utils.subtract(5, 3));
}

performCalculations();
